/**
 * Integration test placeholder — requires running services
 * TODO: Start gateway via test harness and assert /health
 */
import { describe, it } from "node:test";

describe("gateway health", () => {
  it("placeholder — TODO: fetch http://localhost:3000/health", () => {
    // Skipped until test harness is implemented
  });
});
